<Grid
    x:Class="Restless.App.Camera.CameraWallControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:core="clr-namespace:Restless.App.Camera.Core"
    xmlns:local="clr-namespace:Restless.App.Camera"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800">

    <HeaderedContentControl
        x:Name="CameraListHeader" x:FieldModifier="private"
        Header="Available Cameras"
        Margin="2"
        MaxWidth="200"
        core:Property.IsVisible="{Binding IsCameraListVisible}">
        <StackPanel>
            <core:CameraListControl 
                x:Name="CameraList"
                x:FieldModifier="private"
                Style="{StaticResource ListBoxCameraList}" />
            <TextBlock 
                Margin="1,10"
                Foreground="Gray"
                TextWrapping="Wrap"
                Text="To add a camera to the display, drag it to the position you want."/>
        </StackPanel>

    </HeaderedContentControl>

</Grid>
